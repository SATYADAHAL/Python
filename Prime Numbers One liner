print(list(set([0 if([1 if(x%j==0) else 0 for j in range(2,x)].count(1)) else x for x in range(int(input("Enter num of primes you want to print: ")) +1 )]))[2:])

#This is a simple python program to calculate Prime numbers up to nth term. I tried my best to fit the whole program in one line. If you found some better method to do please share it.
